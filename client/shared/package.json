{
  "private": true,
  "name": "@sourcegraph/shared",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "storybook": "STORIES_GLOB='client/shared/src/**/*.story.tsx' pnpm -F @sourcegraph/storybook start",
    "lint:js": "eslint --cache '**/*.[jt]s?(x)'",
    "lint:css": "pnpm stylelint 'src/**/*.scss' --quiet",
    "test": "jest",
    "graphql": "gulp graphQlSchema",
    "schema": "gulp schema",
    "graphql-operations": "gulp graphQlOperations",
    "watch-schema": "gulp watchSchema"
  },
  "sideEffects": true
}
