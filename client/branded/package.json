{
  "private": true,
  "name": "@sourcegraph/branded",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "scripts": {
    "storybook": "STORIES_GLOB='client/branded/src/**/*.story.tsx' pnpm -F @sourcegraph/storybook start",
    "lint:js": "eslint --cache '**/*.[jt]s?(x)'",
    "lint:css": "stylelint 'src/**/*.scss' --quiet",
    "test": "yarn run -T jest"
  }
}
